{% extends 'base2.html.twig' %}

{% block PageContent %}



    <table class="table">
        <tbody>
        {% for offre in offre_hotel  %}
        <tr>
            <th>Id OffreHotel</th>
            <td>{{ offre.id }}</td>
        </tr>
        <tr>
            <th>Nom Hotel</th>
            <td>{{ offre.idH.nom }}</td>
        </tr>
        <tr>
            <th>Nom Offre</th>
            <td>{{ offre.idO.getNomOffre() }}</td>
        </tr>
        <tr>
            <th>Prix Offre</th>
            <td>{{ offre.idO.getPrixOffre() }}</td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('offre_hotel_index') }}">back to list</a>

    <a href="{{ path('offre_hotel_edit', {'id': offre_hotel.id}) }}">edit</a>
    {{ include('offre_hotel/_delete_form.html.twig') }}
{% endblock %}
