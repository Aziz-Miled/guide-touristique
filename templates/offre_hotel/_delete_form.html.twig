{% extends "base2.html.twig" %}
           {% block PageContent %}

               <form method="post" action="{{ path('offre_hotel_delete', {'id': offre_hotel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                   <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_hotel.id) }}">
                   <button class="btn">Delete</button>
               </form>


           {% endblock %}

